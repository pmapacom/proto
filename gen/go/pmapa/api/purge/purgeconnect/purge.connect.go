// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: pmapa/api/purge/purge.proto

// Shared cross-cutting API implemented by every service that stores user-owned
// data (user, travel, post, message, media). Lives under pmapa.api.* — the home
// for contracts many services implement identically. Deliberately UNVERSIONED:
// the contract is trivial and stable (an empty, self-scoped erase), so a version
// suffix would only be noise. If it ever needs to evolve incompatibly, move it
// to pmapa.api.purge.v2.
package purgeconnect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	purge "github.com/pmapacom/proto/gen/go/pmapa/api/purge"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// PurgeServiceName is the fully-qualified name of the PurgeService service.
	PurgeServiceName = "pmapa.api.purge.PurgeService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// PurgeServicePurgeUserProcedure is the fully-qualified name of the PurgeService's PurgeUser RPC.
	PurgeServicePurgeUserProcedure = "/pmapa.api.purge.PurgeService/PurgeUser"
)

// PurgeServiceClient is a client for the pmapa.api.purge.PurgeService service.
type PurgeServiceClient interface {
	PurgeUser(context.Context, *connect.Request[purge.PurgeUserRequest]) (*connect.Response[purge.PurgeUserResponse], error)
}

// NewPurgeServiceClient constructs a client for the pmapa.api.purge.PurgeService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewPurgeServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) PurgeServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	purgeServiceMethods := purge.File_pmapa_api_purge_purge_proto.Services().ByName("PurgeService").Methods()
	return &purgeServiceClient{
		purgeUser: connect.NewClient[purge.PurgeUserRequest, purge.PurgeUserResponse](
			httpClient,
			baseURL+PurgeServicePurgeUserProcedure,
			connect.WithSchema(purgeServiceMethods.ByName("PurgeUser")),
			connect.WithClientOptions(opts...),
		),
	}
}

// purgeServiceClient implements PurgeServiceClient.
type purgeServiceClient struct {
	purgeUser *connect.Client[purge.PurgeUserRequest, purge.PurgeUserResponse]
}

// PurgeUser calls pmapa.api.purge.PurgeService.PurgeUser.
func (c *purgeServiceClient) PurgeUser(ctx context.Context, req *connect.Request[purge.PurgeUserRequest]) (*connect.Response[purge.PurgeUserResponse], error) {
	return c.purgeUser.CallUnary(ctx, req)
}

// PurgeServiceHandler is an implementation of the pmapa.api.purge.PurgeService service.
type PurgeServiceHandler interface {
	PurgeUser(context.Context, *connect.Request[purge.PurgeUserRequest]) (*connect.Response[purge.PurgeUserResponse], error)
}

// NewPurgeServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewPurgeServiceHandler(svc PurgeServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	purgeServiceMethods := purge.File_pmapa_api_purge_purge_proto.Services().ByName("PurgeService").Methods()
	purgeServicePurgeUserHandler := connect.NewUnaryHandler(
		PurgeServicePurgeUserProcedure,
		svc.PurgeUser,
		connect.WithSchema(purgeServiceMethods.ByName("PurgeUser")),
		connect.WithHandlerOptions(opts...),
	)
	return "/pmapa.api.purge.PurgeService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case PurgeServicePurgeUserProcedure:
			purgeServicePurgeUserHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedPurgeServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedPurgeServiceHandler struct{}

func (UnimplementedPurgeServiceHandler) PurgeUser(context.Context, *connect.Request[purge.PurgeUserRequest]) (*connect.Response[purge.PurgeUserResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("pmapa.api.purge.PurgeService.PurgeUser is not implemented"))
}
